// Flutter imports:
import 'package:flutter/material.dart';

class BrandPageInfo extends StatelessWidget {
  const BrandPageInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return ListView(
      shrinkWrap: true,
    );
  }
}
